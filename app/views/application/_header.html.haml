%body
  .header-top
    .header-bottom
      %br
      .col-md-12.text-right
        - if user_signed_in?
          %span
            #{current_user.email}

            = link_to  destroy_user_session_path, :method => :delete, class: 'btn btn-danger' do
              = t('buttons.new.out')
          %br
